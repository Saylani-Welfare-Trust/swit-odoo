<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>
        <!-- ===================== -->
        <!-- Non Cash Receipt -->
        <!-- ===================== -->
        <template id="report_advance_donation_receipt">
            <t t-call="web.basic_layout">
                <t t-foreach="docs" t-as="receipt">
                    <!-- PAGE WRAPPER -->
                    <div class="page" style="position:relative;">

                            <!-- ===== STYLES ===== -->
                            <style>
                                body {
                                    font-family: Arial, sans-serif;
                                    font-size: 11px;
                                }

                                h1 {
                                    font-size: 14px;
                                    font-weight: bold;
                                    margin: 2px 0;
                                }

                                h4 {
                                    font-size: 12px;
                                    margin: 6px 0;
                                    font-weight: bold;
                                }

                                table {
                                    font-size: 11px;
                                    border-collapse: collapse;
                                }

                                th, td {
                                    padding: 3px 4px !important;
                                }

                                .watermark {
                                    position: absolute;
                                    top: 170px;
                                    left: 50%;
                                    transform: translateX(-50%) rotate(-30deg);
                                    font-size: 80px;
                                    color: rgba(200, 200, 200, 0.15);
                                    font-weight: bold;
                                    white-space: nowrap;
                                    pointer-events: none;
                                    z-index: 0;
                                }

                                .text-end {
                                    text-align: right !important;
                                }

                                .text-center {
                                    text-align: center !important;
                                }
                            </style>
                            <!-- ===== COMPANY BANNER ===== -->
                            <!-- Try dn_image first (if exists), then fallback to logo -->
                            <img t-if="receipt.env.company.dn_image" t-att-src="image_data_uri(receipt.env.company.dn_image)" style="display:block; width:100%; height:auto; margin-bottom:6px;" class="company-logo"/>
                            <img t-elif="receipt.env.company.logo" t-att-src="'data:image/png;base64,%s' % receipt.env.company.logo" style="display:block; width:100%; height:auto; margin-bottom:6px;" class="company-logo"/>
                            <img t-elif="receipt.env.company.logo_web" t-att-src="'data:image/png;base64,%s' % receipt.env.company.logo_web" style="display:block; width:100%; height:auto; margin-bottom:6px;" class="company-logo"/>
                            <!-- ===== WATERMARK ===== -->
                            <div t-if="receipt.state == 'draft'" class="watermark">Draft</div>
                            <div t-if="receipt.state == 'pending'" class="watermark">Pending</div>
                            <div t-if="receipt.state == 'bounced'" class="watermark">Bounced</div>

                            <!-- ===== HEADER ===== -->
                            <div class="row mb-2">
                                <div class="col-6">
                                    <h1>Non Cash Receipt</h1>
                                </div>
                                <div class="col-6 text-end">
                                    <h1>
                                        <t t-esc="receipt.env.company.name or 'Saylani Welfare International Trust'"/>
                                    </h1>
                                </div>
                            </div>

                            <hr style="margin:4px 0"/>

                            <!-- ===== BASIC INFO ===== -->
                            <div class="row mb-2">
                                <div class="col-6">
                                    <div><strong>Receipt #:</strong> <t t-esc="receipt.name or '--'"/></div>
                                    <div name="donor_name"><strong>Donor Name:</strong> <t t-esc="receipt.donor_id.name or '-'"/></div>
                                    <!-- Donor Mobile Number -->
                                    <div t-if="receipt.donor_id.mobile">
                                        <strong>Mobile Number:</strong> <t t-esc="receipt.donor_id.mobile or '-'"/>
                                    </div>
                                    <div><strong>Donation:</strong> <t t-esc="receipt.donation_id.name or '-'"/></div>
                                </div>
                                <div class="col-6">
                                    <!-- Category Field -->
                                    <div t-if="receipt.category_id">
                                        <strong>Category:</strong> <t t-esc="receipt.category_id.display_name or '-'"/>
                                    </div>
                                    <!-- Product Name Field -->
                                    <div t-if="receipt.product_id">
                                        <strong>Product Name:</strong> <t t-esc="receipt.product_id.display_name or '-'"/>
                                    </div>
                                    <div><strong>Payment Type:</strong> <t t-esc="dict(receipt.fields_get(allfields=['payment_type'])['payment_type']['selection'])[receipt.payment_type]"/></div>
                                    <div>
                                        <strong>Date:</strong>
                                        <t t-esc="receipt.date.strftime('%d-%m-%Y') if receipt.date else '--'"/>
                                    </div>
                                    <div>
                                        <strong>Status:</strong>
                                        <t t-if="receipt.state == 'draft'">Draft</t>
                                        <t t-elif="receipt.state == 'paid'" style="color: green; font-weight: bold;">Paid</t>
                                        <t t-elif="receipt.state == 'pending'" style="color: orange; font-weight: bold;">Pending</t>
                                        <t t-elif="receipt.state == 'bounced'" style="color: red; font-weight: bold;">Bounced</t>
                                        <t t-else="" t-esc="receipt.state"/>
                                    </div>
                                    <div t-if="receipt.payment_type == 'cheque'">
                                        <strong>Cheque #:</strong> <t t-esc="receipt.cheque_number or '-'"/>
                                    </div>
                                    <div t-if="receipt.payment_type == 'cheque'">
                                        <strong>Cheque Date:</strong> 
                                        <t t-esc="receipt.cheque_date.strftime('%d-%m-%Y') if receipt.cheque_date else '--'"/>
                                    </div>
                                    <div t-if="receipt.payment_type == 'cheque' and receipt.bank_id">
                                        <strong>Bank:</strong> <t t-esc="receipt.bank_id.name"/>
                                    </div>
                                    <div t-if="receipt.state == 'bounced' and receipt.bounced_reason">
                                        <strong>Bounced Reason:</strong> <t t-esc="receipt.bounced_reason"/>
                                    </div>
                                    <!-- POS Session Info -->
                                    <div t-if="receipt.pos_session_id">
                                        <strong>POS Session:</strong> <t t-esc="receipt.pos_session_id.name or '-'"/>
                                    </div>
                                    <div t-if="receipt.pos_order_id">
                                        <strong>POS Order:</strong> <t t-esc="receipt.pos_order_id.name or '-'"/>
                                    </div>
                                </div>
                            </div>

                            <!-- ===== AMOUNT CARDS ===== -->
                            <h5 style="color: #2E4053; border-bottom: 1px solid #2E4053; margin-top:8px;">Financial Details</h5>
                            <table style="width:100%; margin:0 0 6px 0;">
                                <tr>
                                    <td style="width:33%; padding:0 4px 0 0; vertical-align:top;">
                                        <div style="border:2px solid #3498DB; padding:8px;">
                                            <div style="color:#3498DB; font-size:10px; font-weight:bold;">Total Amount</div>
                                            <div style="color:#2E4053; font-size:14px; font-weight:bold;"><t t-esc="'{:,.2f}'.format(receipt.amount or 0)"/> <t t-esc="receipt.currency_id.symbol"/></div>
                                        </div>
                                    </td>
                                    <td style="width:33%; padding:0 2px; vertical-align:top;">
                                        <div style="border:2px solid #27AE60; padding:8px;">
                                            <div style="color:#27AE60; font-size:10px; font-weight:bold;">Used Amount</div>
                                            <div style="color:#27AE60; font-size:14px; font-weight:bold;"><t t-esc="'{:,.2f}'.format(receipt.used_amount or 0)"/> <t t-esc="receipt.currency_id.symbol"/></div>
                                        </div>
                                    </td>
                                    <td style="width:33%; padding:0 0 0 4px; vertical-align:top;">
                                        <div style="border:2px solid #E74C3C; padding:8px;">
                                            <div style="color:#E74C3C; font-size:10px; font-weight:bold;">Remaining Amount</div>
                                            <div style="color:#E74C3C; font-size:14px; font-weight:bold;"><t t-esc="'{:,.2f}'.format(receipt.remaining_amount or 0)"/> <t t-esc="receipt.currency_id.symbol"/></div>
                                        </div>
                                    </td>
                                </tr>
                            </table>

                            <!-- ===== REMARKS SECTION (from POS) ===== -->
                            <div t-if="receipt.pos_order_id and receipt.pos_order_id.note" style="margin-top: 10px; padding: 8px; border: 1px solid #ddd; background-color: #f9f9f9;">
                                <strong>Remarks:</strong>
                                <div style="margin-top: 5px; font-style: italic;">
                                    <t t-esc="receipt.pos_order_id.note or '-'"/>
                                </div>
                            </div>
                            <!-- Alternative: if remarks is stored directly on receipt -->
                            <div t-if="not receipt.pos_order_id and receipt.remarks" style="margin-top: 10px; padding: 8px; border: 1px solid #ddd; background-color: #f9f9f9;">
                                <strong>Remarks:</strong>
                                <div style="margin-top: 5px; font-style: italic;">
                                    <t t-esc="receipt.remarks or '-'"/>
                                </div>
                            </div>

                            <hr style="margin:4px 0"/>

                            <!-- ===== TOTALS ===== -->
                            <table style="width:100%; margin-top:10px; font-size:10px;">
                                <tr>
                                    <td style="text-align:left; padding:0;">Generated by: <strong><t t-esc="receipt.create_uid.name or receipt.user_id.name or ''"/></strong></td>
                                    <td style="text-align:right; padding:0;">Total Amount: <strong>Rs <t t-esc="'{:,.2f}'.format(receipt.amount or 0)"/></strong></td>
                                </tr>
                            </table>

                            <hr style="margin:4px 0; border-top: solid 5px;"/>

                            <!-- ===== FOOTER DETAILS ===== -->
                            <div class="row mb-2">
                                <div class="col-6">
                                    <div><strong>Receipt #:</strong> <t t-esc="receipt.name or '--'"/></div>
                                    <div><strong>Donor Name:</strong> <t t-esc="receipt.donor_id.name or '-'"/></div>
                                    <!-- Donor Mobile Number in Footer -->
                                    <div t-if="receipt.donor_id.mobile">
                                        <strong>Mobile Number:</strong> <t t-esc="receipt.donor_id.mobile"/>
                                    </div>
                                    <div>
                                        <t t-if="receipt.donation_id">
                                            <strong>Donation:</strong> <t t-esc="receipt.donation_id.name"/>
                                        </t>
                                    </div>
                                    <!-- Category in Footer -->
                                    <div t-if="receipt.category_id">
                                        <strong>Category:</strong> <t t-esc="receipt.category_id.display_name"/>
                                    </div>
                                    <div t-if="receipt.product_id">
                                        <strong>Product Name:</strong> <t t-esc="receipt.product_id.display_name"/>
                                    </div>
                                    <div t-if="receipt.payment_type == 'cheque'">
                                        <strong>Cheque #:</strong> <t t-esc="receipt.cheque_number or '-'"/>
                                    </div>
                                    <!-- POS Order in Footer -->
                                    <div t-if="receipt.pos_order_id">
                                        <strong>POS Order:</strong> <t t-esc="receipt.pos_order_id.name"/>
                                    </div>
                                </div>
                                <div class="col-6">
                                    <div>
                                        <strong>Date:</strong>
                                        <t t-esc="receipt.date.strftime('%d-%m-%Y') if receipt.date else '--'"/>
                                    </div>
                                    <div><strong>Generated by: </strong> <t t-esc="receipt.create_uid.name or receipt.user_id.name or ''"/></div>
                                    <div>
                                        <strong>Status:</strong>
                                        <t t-if="receipt.state == 'draft'">Draft</t>
                                        <t t-elif="receipt.state == 'paid'" style="color: green; font-weight: bold;">✓ Paid</t>
                                        <t t-elif="receipt.state == 'pending'" style="color: orange; font-weight: bold;">⏳ Pending</t>
                                        <t t-elif="receipt.state == 'bounced'" style="color: red; font-weight: bold;">✗ Bounced</t>
                                    </div>
                                    <div t-if="receipt.state == 'paid'">
                                        <strong>Payment Confirmed:</strong> 
                                        <t t-esc="receipt.write_date.strftime('%d-%m-%Y %H:%M') if receipt.write_date else '--'"/>
                                    </div>
                                    <!-- Remarks in Footer (from POS) -->
                                    <div t-if="receipt.pos_order_id and receipt.pos_order_id.note">
                                        <strong>Remarks:</strong> <t t-esc="receipt.pos_order_id.note"/>
                                    </div>
                                    <!-- Alternative: if remarks is stored directly on receipt -->
                                    <div t-if="not receipt.pos_order_id and receipt.remarks">
                                        <strong>Remarks:</strong> <t t-esc="receipt.remarks"/>
                                    </div>
                                </div>
                            </div>

                            <!-- ===== FOOTER ===== -->
                            <div style="text-align: center; font-size: 10px; margin-top: 10px; color: #666;">
                                <span style="font-size: 8px;">This is a computer-generated receipt. No physical signature required.</span>
                                <br/>
                                <span style="font-size: 8px;">Powered by BytesNode</span>
                            </div>

                    </div>
                </t>
            </t>
        </template>

        <!-- ===================== -->
        <!-- Paper Format -->
        <!-- ===================== -->
        <record id="report_advance_donation_receipt_paperformat" model="report.paperformat">
            <field name="name">A5 - Non Cash Receipt</field>
            <field name="format">A5</field>
            <field name="orientation">Portrait</field>
            <field name="margin_top">0</field>
            <field name="margin_bottom">10</field>
            <field name="margin_left">4</field>
            <field name="margin_right">4</field>
            <field name="header_spacing">0</field>
            <field name="dpi">90</field>
        </record>

        <!-- ===================== -->
        <!-- Report Action -->
        <!-- ===================== -->
        <record id="action_report_advance_donation_receipt" model="ir.actions.report">
            <field name="name">Non Cash Receipt</field>
            <field name="model">advance.donation.receipt</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">bn_advance_donation.report_advance_donation_receipt</field>
            <field name="report_file">bn_advance_donation.report_advance_donation_receipt</field>
            <field name="binding_model_id" ref="model_advance_donation_receipt"/>
            <field name="binding_type">report</field>
            <field name="print_report_name">'Non Cash Receipt - %s' % (object.name)</field>
            <field name="paperformat_id" ref="report_advance_donation_receipt_paperformat"/>
        </record>

    </data>
</odoo>