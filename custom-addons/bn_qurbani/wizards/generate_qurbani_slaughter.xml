<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="generate_qurbani_slaughter_view_form" model="ir.ui.view">
        <field name="name">generate.qurbani.slaughter.view.form</field>
        <field name="model">generate.qurbani.slaughter</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="inventory_product_name" invisible="1" />

                        <group name="generate_slaughter_col_1">
                            <field name="day_id" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                            <field name="hijri_id" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                            <field name="slaughter_location_id" domain="[('is_slaughter_location', '=', True)]" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                        </group>
                        <group name="generate_slaughter_col_1">
                            <field name="inventory_product_id" domain="[('type', '=', 'product'), ('is_qurbani_inventory', '=', True), ('is_livestock', '=', True)]" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                            <field name="pos_product_id" domain="[('name', 'ilike', inventory_product_name), ('type', '=', 'product'), ('is_qurbani_inventory', '=', False), ('is_livestock', '=', True)]" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button string="Generate" type="object" name="action_generate_slaughter" class="btn-primary"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="generate_qurbani_slaughter_action" model="ir.actions.act_window">
        <field name="name">Generate Qurbani Slaughter</field>
        <field name="res_model">generate.qurbani.slaughter</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Let's create a new generate qurbani slaughter....
            </p>
        </field>
    </record>

    <menuitem
        id="generate_qurbani_slaughter_menu"
        name="Generate Qurbani Slaughter"
        action="generate_qurbani_slaughter_action"
        parent="bn_qurbani.qurbani_coniguration_menu"
        sequence="1"/>

</odoo>
