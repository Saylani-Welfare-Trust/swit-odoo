<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="qurbani_city_demand_view_form" model="ir.ui.view">
        <field name="name">qurbani.city.demand.view.form</field>
        <field name="model">qurbani.city.demand</field>
        <field name="arch" type="xml">
            <form create="0" edit="0">
                <sheet>
                    <div class="oe_title">
                        <label for="day_id" />
                        <h1><field name="day_id" readonly="1" options="{'no_create': True, 'no_edit': True, 'no_open': True}" /></h1>
                    </div>

                    <group>
                        <group name="qurbani_city_demand_col_1">
                            <field name="hijri_id" readonly="1" options="{'no_create': True, 'no_edit': True, 'no_open': True}"/>
                            <field name="city_location_id" readonly="1" options="{'no_create': True, 'no_edit': True, 'no_open': True}"/>
                            <field name="demand"/>
                            <field name="total_demand" readonly="1"/>
                        </group>
                        <group name="qurbani_city_demand_col_1">
                            <field name="slaughter_location_ids" domain="[('is_slaughter_location', '=', True)]" widget="many2many_tags" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                            <field name="inventory_product_id" domain="[('type', '=', 'product'), ('is_qurbani_inventory', '=', True), ('is_livestock', '=', True)]" options="{'no_create': True, 'no_edit': True, 'no_open': True}" readonly="1"/>
                            <field name="remaining_demand" readonly="1"/>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="qurbani_city_demand_view_tree" model="ir.ui.view">
        <field name="name">qurbani.city.demand.view.tree</field>
        <field name="model">qurbani.city.demand</field>
        <field name="arch" type="xml">
            <tree create="0" editable="bottom">
                <field name="day_id" readonly="1"/>
                <field name="hijri_id" readonly="1"/>
                <field name="city_location_id" readonly="1"/>
                <field name="slaughter_location_ids" domain="[('is_slaughter_location', '=', True)]" widget="many2many_tags" options="{'no_create': True, 'no_edit': True, 'no_open': True}" required="1"/>
                <field name="inventory_product_id" domain="[('type', '=', 'product'), ('is_qurbani_inventory', '=', True), ('is_livestock', '=', True)]" options="{'no_create': True, 'no_edit': True, 'no_open': True}" readonly="1"/>
                <field name="demand"/>
                <field name="total_demand" readonly="1"/>
                <field name="remaining_demand" readonly="1"/>

                <button string="History" name="action_open_chatter" type="object" class="oe_highlight"/>
            </tree>
        </field>
    </record>

    <record id="qurbani_city_demand_action" model="ir.actions.act_window">
        <field name="name">City</field>
        <field name="res_model">qurbani.city.demand</field>
        <field name="view_mode">tree</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Let's create a new city demand....
            </p>
        </field>
    </record>

    <menuitem
        id="qurbani_demand_menu"
        name="Qurbani Demand"
        parent="bn_qurbani.menu_qurbani_root"
        sequence="2"/>

    <menuitem
        id="qurbani_city_demand_menu"
        name="City"
        action="qurbani_city_demand_action"
        parent="qurbani_demand_menu"
        sequence="0"/>

</odoo>
