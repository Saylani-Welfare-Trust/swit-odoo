<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">

        <record id="update_country_name" model="ir.actions.server">
            <field name="name">Update Country Name | BN</field>
            <field name="model_id" ref="base.model_res_country"/>
            <field name="state">code</field>
            <field name="code">
for rec in records:
    rec['name'] = f"{rec.name} (+{rec.phone_code})"
            </field>
        </record>

    </data>

</odoo>
