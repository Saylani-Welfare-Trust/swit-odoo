<odoo>
    <record id="ir_cron_recurring_monthly" model="ir.cron">
        <field name="name">Add Recurring Monthly Requirements</field>
        <field name="model_id" ref="model_ration_monthly_req"/>
        <field name="state">code</field>
        <field name="code">
            model._add_recurring_requirements()
        </field>
        <field name="interval_number">1</field>
        <field name="interval_type">months</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(day=1)).strftime('%Y-%m-%d 00:00:00')"/>
    </record>


    <record id="ir_cron_build_daily_req" model="ir.cron">
        <field name="name">Build Next‑Day Daily Requirements</field>
        <field name="model_id" ref="model_ration_daily_req"/>
        <field name="state">code</field>
        <field name="code">
            model._build_from_pos_orders()
        </field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now().strftime('%Y-%m-%d') + ' 12:00:00')"/>
    </record>
</odoo>
