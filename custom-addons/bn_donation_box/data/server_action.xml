<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <record id="bulk_install_server_action" model="ir.actions.server">
            <field name="name">Bulk Install | BN</field>
            <field name="model_id" ref="bn_donation_box.model_donation_box_registration_installation"/>
            <field name="state">code</field>
            <field name="code">
model.install_donation_box(records)
            </field>
        </record>
        
        <record id="add_demo_rider_server_action" model="ir.actions.server">
            <field name="name">Add Demo Rider | BN</field>
            <field name="model_id" ref="bn_donation_box.model_donation_box_registration_installation"/>
            <field name="state">code</field>
            <field name="code">
model.add_demo_rider(records)
            </field>
        </record>
        
        <record id="bulk_approved_server_action" model="ir.actions.server">
            <field name="name">Bulk Approved | BN</field>
            <field name="model_id" ref="bn_donation_box.model_donation_box_registration_installation"/>
            <field name="state">code</field>
            <field name="code">
model.approve_donation_box(records)
            </field>
        </record>
        
        <record id="populate_lock_no_server_action" model="ir.actions.server">
            <field name="name">Populate Lock No | BN</field>
            <field name="model_id" ref="bn_donation_box.model_donation_box_registration_installation"/>
            <field name="state">code</field>
            <field name="code">
model.populate_lock_no(records)
            </field>
        </record>

    </data>

</odoo>
