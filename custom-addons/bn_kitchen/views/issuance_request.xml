<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="issuance_request_action" model="ir.actions.act_window">
        <field name="name">Issuance Request</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('is_issuance_request', '=', True)]</field>
        <field name="context">{'default_is_issuance_request': True, 'contact_display': 'partner_address', 'default_company_id': allowed_company_ids[0], 'restricted_picking_type_code': 'internal', 'search_default_internal': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No issuance request found. Let's create one!

                Issuance Request allow you to move products from one location to another. 
            </p>
        </field>
    </record>

    <menuitem
        id="issuance_request_menu"
        name="Issuance Request"
        action="issuance_request_action"
        parent="bn_kitchen.kitchen_menu"
        sequence="1"/>

</odoo>
