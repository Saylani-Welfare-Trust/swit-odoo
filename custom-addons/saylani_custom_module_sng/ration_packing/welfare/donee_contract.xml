<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Tree View for Donee Contract -->
    <record id="view_donee_contract_tree" model="ir.ui.view">
        <field name="name">donee.contract.tree</field>
        <field name="model">donee.contract</field>
        <field name="arch" type="xml">
            <tree string="Donee Contracts">
                <field name="name"/>
                <field name="center_id"/>
                <field name="recurring"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Form View for Donee Contract -->
    <record id="view_donee_contract_form" model="ir.ui.view">
        <field name="name">donee.contract.form</field>
        <field name="model">donee.contract</field>
        <field name="arch" type="xml">
            <form string="Donee Contract">
                <header>

                    <field name="state" widget="statusbar" statusbar_visible="draft,approved,'cancelled'"/>

                    <button name="action_approve" type="object" string="Approve" class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state != 'approved'"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="center_id"/>
                        <field name="recurring"/>
                    </group>
                    <notebook>
                        <page string="Contract Lines">
                            <field name="pack_line_ids">
                                <tree editable="bottom">
                                    <field name="category_id"/>
                                    <field name="product_id" invisible="1"/>
                                    <field name="quantity"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Views for Donee Contract Line -->
    <record id="view_donee_contract_line_tree" model="ir.ui.view">
        <field name="name">donee.contract.line.tree</field>
        <field name="model">donee.contract.line</field>
        <field name="arch" type="xml">
            <tree string="Contract Lines">
                <field name="contract_id"/>
                <field name="category_id"/>
                <field name="quantity"/>
            </tree>
        </field>
    </record>

    <!-- Action for Donee Contracts -->
    <record id="action_donee_contract" model="ir.actions.act_window">
        <field name="name">Donee Contracts</field>
        <field name="res_model">donee.contract</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Donee Contract
            </p>
        </field>
    </record>


    <record id="seq_donee_contract_no" model="ir.sequence">
        <field name="name">Donee Contract No.</field>
        <field name="code">donee.contract</field>
        <!-- No prefix here since we’ll build it in the Python code -->
        <field name="prefix">Donee-</field>
        <field name="padding">6</field>
        <field name="company_id" eval="False"/>
    </record>


</odoo>
