<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="closing_screen_view_form" model="ir.ui.view">
        <field name="name">wizard.closing.screen.view.form</field>
        <field name="model">wizard.closing.screen</field>
        <field name="arch" type="xml">
            <form string="">
                <sheet>
                    <group>
                        <group>
                            <field name="name" required="1"/>
                            <field name="pos_session" readonly="1"/>
                            
                        </group>
                        
                    </group>
                    <field name="line_id">
                        <tree editable="bottom">
                            <field name="pos_category"/>
                            <field name="payment_method"/>
                            <field name="amount"/>
                            <field name="deposit_slip"/>
                        </tree>
                    </field>
                </sheet>
                    <footer>

                <button string="Save" type="object" name="action_save" class="btn-primary"/>
                <button string="Cancel" class="btn-secondary" special="cancel"/>
            </footer>
             
            </form>
        </field>
    </record>


   <record id="action_closing_screen" model="ir.actions.act_window">
        <field name="name">Close session</field>
        <field name="res_model">wizard.closing.screen</field>
       <field name="view_id" ref="closing_screen.closing_screen_view_form"/>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>





        <menuitem id="menu_closing_screen"
          name="Close Session Screen"
          parent="point_of_sale.menu_point_root"
          sequence="110"
          action="action_closing_screen"
          />


</odoo>
