<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="correct_records_import_donation_action_server" model="ir.actions.server">
        <field name="name">Correct Records Import Donation | BN</field>
        <field name="model_id" ref="model_import_donation"/>
        <field name="binding_model_id" ref="model_import_donation"/>
        <field name="state">code</field>
        <field name="code">
model.action_correct_records()
        </field>
    </record>

    <record id="sync_existing_donation_donors_action_server" model="ir.actions.server">
        <field name="name">Sync Donors for Existing Donations</field>
        <field name="model_id" ref="model_donation"/>
        <field name="binding_model_id" ref="model_donation"/>
        <field name="state">code</field>
        <field name="code">action = records.action_sync_existing_donors()</field>
    </record>

</odoo>
