<?xml version="1.0" encoding="UTF-8"?>

<templates id="template" xml:space="preserve">
    <t t-inherit="point_of_sale.ProductScreen" t-inherit-mode="extension">
        <xpath expr="//OrderWidget" position="attributes">
            <attribute name="lines">getVisibleOrderlines()</attribute>
            <attribute name="total">env.utils.formatCurrency(getVisibleOrderTotal())</attribute>
            <attribute name="tax">!env.utils.floatIsZero(getVisibleOrderTax()) and env.utils.formatCurrency(getVisibleOrderTax()) or ''</attribute>
        </xpath>
    </t>
</templates>